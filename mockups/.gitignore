# Built mockups. Every dashboard-v6-* page is generated from its .src.html by
# the matching build script, so only the source is worth tracking. This was a
# list of individual filenames and a new mockup kept slipping past it; the
# pattern plus the negation below covers the next one too.
dashboard-v6*.html
!dashboard-v6*.src.html
